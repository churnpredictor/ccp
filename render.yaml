services:
  - type: web
    name: churn-predictor
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --preload server:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
